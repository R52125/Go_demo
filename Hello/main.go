package main

import (
	"fmt"
)

func main() {
	var Str string = "1111"
	if Str == "1111" {
		fmt.Printf("chenboren is sb")
 		fmt.Printf("You are a bad boy")
	} else {
		fmt.Printf("chenboren is genius")
		fmt.Printf("You are a bad girl")
	}
	// slice
	// map
	// range
	// struct
	// struct类成员函数
	// error
	// string operation: Contains Count HasPrefix HasSuffix Index Join Repeat Replace Split ToLower ToUpper len
	// string format: %v可以打印任意类型的变量，%+v可以得到更详细的结果，%#v进一步详细
	// json
	// time time.Now() Format
	// 数字解析
	// 进程信息
}
